apply plugin: 'maven'

repositories {
    maven {
        name = "Maven Repository"
        url = "${project.artifactsURL}${project.artifactsRepoKey}".toURL()
    }
    ivy {
        name = "Ivy Repository"
        url = "${project.artifactsURL}${project.artifactsRepoKey}".toURL()
    }
}